export default function ActiveSession() {
  return (
    <section>
      <h1 className="text-2xl font-bold">Active Session</h1>
      <p className="mt-2 text-gray-600">Full-screen countdown for the chosen app, then a "time's up" state that must be acknowledged.</p>
      <p className="mt-4 text-sm text-gray-400">Placeholder — spec section 4.6</p>
    </section>
  )
}
